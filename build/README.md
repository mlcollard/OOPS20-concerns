# Build

In the directory build (this directory), run the following:

```console
cmake ..
```

To build:

```console
make
```

To run with example:

```console
make run
```

To clean generated object and executables:

```console
make clean
```
